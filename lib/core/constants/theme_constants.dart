// Color schemes and dimensions
// lib/core/constants/theme_constants.dart

import 'package:flutter/material.dart';

class ThemeConstants {
  static const Color primaryColor = Colors.blue;
  static const Color accentColor = Colors.lightBlueAccent;
  static const TextStyle headingTextStyle = TextStyle(fontSize: 24, fontWeight: FontWeight.bold);
  // Define other theme-related constants like text styles, colors, etc.
}